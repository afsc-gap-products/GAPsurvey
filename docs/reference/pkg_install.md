# Package install

Package install

## Usage

``` r
pkg_install(PKG)
```

## Arguments

- PKG:

  name of package this script has additional capability to work on
  google workstations. You can use the library install bash script.
